# Edit the following content in the head
- The title of the Page
- Put a description in the head
- Include SEO meta content in the head
- Change favicon to brand logo in the head

# Edit the following in the header
- Place the navigation menu in the center
- Remove the bottom border line of the header

- Add "Pricing" to the navigation menu items between "Use Cases" and "Log in"

- Remove the login link from the header and only have the get started button

- confirm that the typography in the header is making use of the inter font family
- confirm that the button color is using the primary color in the color system
- Confirm that on primary color is in the color system and ask the Agent to use in on all the primary button text

- Add horizontal padding of 6rem in the header
- Ask the agent to configure box-sizing : border-box in the the global settings of the page

- Remove the text decoration on the navigation links
- Ask the AI Agent to use the body large text for the nav links

# Hero section
- Remove every section below the hero section including the footer
- Remove the border-bottom line of the hero section

- Change the blue button from "Start for free ➔" to "Get Started for Free" and remove the arrow icon.
- Remove the learn more button from the hero section and center the get started for free button

- Ask the AI Agent to use the display large text for the hero headline text

- Remove the radial gradient background at the center of the hero section
- Remove the pill badge "WhatsApp Business API Ready" sitting above the headline

- Change the Subheadline text to a warm phrase 

Example of warm phrase:
- Turn social media leads into warm conversations instantly. Connect your WhatsApp Business API, import contacts, and launch high-converting outreach campaigns at scale.
- Transform social media leads into real-time engagement the moment they land. Connect your WhatsApp Business API, import your contacts, and launch high-converting, compliant campaigns effortlessly.

- Ask the Agent to go to the color system and pick a not-very-black color for the hero description text
- Ask the agent to use 100vh for the height of the whole marketing page

- Ask the AI Agent to use the headline large text for the section headings

- Update the main hero button to use the exact vertical padding as the header button.

- Reduce the roundness of the two buttons into sharp rounded rectangle in the landing page

- Constrain the hero description text's width so that the text wraps into exactly two centered lines.

# Signup page - Experience problems
- Navigation : Add a home link on the page. You can use the logo of the app to represent that

- Update the labels to Company Name, Work Email

- Input fields need hover interaction states

# Sign up page - UI Problems
- The button label is not using the on primary color
- Remove form border
- Reduce default border blackness of the input field
- Reduce the blackness of the label texts

- Remove bottom border of the header
- Remove all placeholders 
- Change form title to sign up
- Remove form description

- Change form and button text to Create Account
- Add a margin of 32px between form title and form
- Add animation to input focus(On the sign up form, when a user clicks inside an input and the input field is in focus, add a drop shadow effect to the input border in focus, make it soft and add a transition of 1second)

- Set the outline variant color variable to neutralVariant 80 and use it as the default border color of the input field

- Cursor behaviour(pointer)

# Form states
- Empty Input states

# Company Name field

- Prevent special characters
- Minimum of 2 characters 
- If a user goes into focus mode and leaves the input without typing their company name, display an error in red saying "Field must not be empty". Remove the error only when they start typing in the input field

- Display "Enter a valid company email address" the moment the user starts typing in the email field. Remove the error message only when the extension in the email address that follows the @ symbol does not end with "gmail.com" or "yahoo.com". 

- For the password requirements display this in real time
* Password must be 8 characters
* Password must contain a number
* Password must contain a special character
* Password must contain an uppercase letter

Do not display all the password requirements at once. Display them as the user types in the password field and as they meet each requirement, display a green checkmark next to it. 

# Onboarding Phase 2
- Change the button text on the continue button from continue to next with an arrow pointing to the right
- Create a color visual hierarchy between the Let's get you set up title and the description text below it. Let the description use a light gray color from the color system
- Remove the border from the whole welcome to send signal component. Remove all the black border lines
- For all the buttons using the primary color, let the texts and icons on the buttons use the on primary color

- Reduce the width of the form

- Reduce the font weight of the description text a bit 
